{
  "name": "daily-wird",
  "version": "1.0.0",
  "private": true,
  "description": "Shared daily wird checklist for two, with a persistent backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "redis": "^4.7.0"
  }
}
